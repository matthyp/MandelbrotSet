function Plot(x, y)
{
        X = x;
        Y = -y;
        return [x, y]
}

function Graph(x, y)
{
        return Plot(x * 150 + 540, y * 150 + 360)
}

function f(x, y, cx, cy)
{
        return Graph(x * x - y * y + cx, 2 * x * y + cy)
}
